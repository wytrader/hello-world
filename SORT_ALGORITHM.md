# sort_algorithm.py 使用文档

`sort_algorithm.py` 是一个示例排序程序，使用 **归并排序（Merge Sort）** 对整数列表进行升序排序。

## 功能概览

- 提供 `merge_sort(nums)`：返回新的升序列表，不修改原输入。
- 提供命令行交互入口：读取一行空格分隔整数并输出排序结果。
- 使用类型注解，代码结构清晰，便于学习和扩展。

## 文件位置

- `sort_algorithm.py`

## 算法说明

归并排序采用“分治法”：

1. 将数组递归拆分成左右两半，直到子数组长度为 1（或 0）。
2. 将两个有序子数组合并为一个新的有序数组。
3. 最终得到整体有序结果。

### 复杂度

- 时间复杂度：`O(n log n)`
- 空间复杂度：`O(n)`（需要额外合并数组）
- 稳定性：稳定排序（相等元素相对次序不变）

## 快速开始

### 1) 运行脚本

```bash
python3 sort_algorithm.py
```

### 2) 输入示例

输入：

```text
3 1 2 9 5
```

输出：

```text
排序结果： [1, 2, 3, 5, 9]
```

## 代码接口

### `merge_sort(nums: List[int]) -> List[int]`

- 参数：整数列表
- 返回：新的升序列表

示例：

```python
from sort_algorithm import merge_sort

result = merge_sort([5, 2, 8, 1])
print(result)  # [1, 2, 5, 8]
```

## 输入注意事项

- 输入必须是整数，且以空格分隔。
- 若输入为空行，程序会输出空列表 `[]`。
- 若包含非整数字符，将抛出 `ValueError`。

## 后续可扩展方向

- 支持降序排序参数。
- 支持从命令行参数传入数据（非交互）。
- 增加单元测试（如 `pytest`）。
- 增加异常处理与更友好的错误提示。
